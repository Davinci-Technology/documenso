---
title: Webhooks
description: Learn how to use webhooks to receive real-time notifications about your documents.
---

# Webhooks

Webhooks are HTTP callbacks triggered by specific events. When you subscribe to a specific event and that event occurs, the webhook makes an HTTP request to the URL you provide. The request can be a simple notification or carry a payload with more information about the event.

Some of the common use cases for webhooks include:

1. **Real-time Data Syncing**: Webhooks provide a way to keep data in sync across different platforms. For example, you can keep your system up-to-date with your Davinci Sign documents by subscribing to events like document creation or signing.
2. **Automating Workflows**: They can trigger automated workflows that start when an event occurs. For example, the webhook could trigger an email when a document is signed.
3. **Integrating Third-Party Services**: Webhooks can be used to integrate Davinci Sign with third-party services. For example, you could use a webhook to send data to a CRM system when a document is signed.

Davinci Sign supports Webhooks and allows you to subscribe to the following events:

- `document.created`
- `document.sent`
- `document.opened`
- `document.signed`
- `document.completed`
- `document.rejected`
- `document.cancelled`

## Create a webhook subscription

You can create a webhook subscription from the team settings page. Click your avatar in the top right corner of the dashboard and select "Team settings" from the dropdown menu.

![A screenshot of the Davinci Sign's dashboard that shows the dropdown menu when you click on your user avatar](/webhook-images/documenso-main-page.webp)

Then, navigate to the "Webhooks" tab, which takes you to the webhooks main page.

![A screenshot of the Davinci Sign's team settings page that shows the Webhooks tab and the Create Webhook button](/webhook-images/webhooks-main-page.webp)

Clicking on the "**Create Webhook**" button opens a modal to create a new webhook subscription.

To create a new webhook subscription, you need to provide the following information:

- Enter the webhook URL that will receive the event payload.
- Select the event(s) you want to subscribe to: `document.created`, `document.sent`, `document.opened`, `document.signed`, `document.completed`, `document.rejected`, `document.cancelled`.
- Optionally, you can provide a secret key that will be used to sign the payload. This key will be included in the `X-Davinci-Secret` header of the request.

![A screenshot of the Create Webhook modal that shows the URL input field and the event checkboxes](/webhook-images/create-webhook-dialog.webp)

After you have filled in the required information, click on the "**Create Webhook**" button to save your subscription.

The screenshot below illustrates a newly created webhook subscription.

![A screenshot of the Davinci Sign's user settings page that shows the newly created webhook subscription](/webhook-images/webhooks-page.webp)

You can edit, view the logs, or delete your webhook subscriptions by clicking the three dots (...) under the "Action" column. You can also access the webhook logs by clicking on the webhook subscription directly.

![A screenshot of the Davinci Sign's team settings page that shows the webhook logs](/webhook-images/webhook-detail-page.webp)

You can go even further and check the execution details of each call by clicking on a specific webhook call.

![A screenshot of the Davinci Sign's team settings page that shows the webhook call details](/webhook-images/webhook-run-page.webp)

This page shows the details of the webhook call such as:

- status
- event
- date when the webhook was sent
- response code
- request body
- response body and headers

## Webhook fields

The payload sent to the webhook URL contains the following fields:

| Field                                        | Type      | Description                                           |
| -------------------------------------------- | --------- | ----------------------------------------------------- |
| `event`                                      | string    | The type of event that triggered the webhook.         |
| `payload.id`                                 | number    | The id of the document.                               |
| `payload.externalId`                         | string?   | External identifier for the document.                 |
| `payload.userId`                             | number    | The id of the user who owns the document.             |
| `payload.authOptions`                        | json?     | Authentication options for the document.              |
| `payload.formValues`                         | json?     | Form values for the document.                         |
| `payload.visibility`                         | string    | Document visibility (e.g., EVERYONE).                 |
| `payload.title`                              | string    | The title of the document.                            |
| `payload.status`                             | string    | The current status of the document.                   |
| `payload.documentDataId`                     | string    | The identifier for the document data.                 |
| `payload.createdAt`                          | datetime  | The creation date and time of the document.           |
| `payload.updatedAt`                          | datetime  | The last update date and time of the document.        |
| `payload.completedAt`                        | datetime? | The completion date and time of the document.         |
| `payload.deletedAt`                          | datetime? | The deletion date and time of the document.           |
| `payload.teamId`                             | number?   | The id of the team if document belongs to a team.     |
| `payload.templateId`                         | number?   | The id of the template if created from template.      |
| `payload.source`                             | string    | The source of the document.                           |
| `payload.documentMeta.subject`               | string?   | The subject of the document email.                    |
| `payload.documentMeta.message`               | string?   | The message of the document email.                    |
| `payload.documentMeta.timezone`              | string?   | The timezone for the document.                        |
| `payload.documentMeta.dateFormat`            | string?   | The date format for the document.                     |
| `payload.documentMeta.redirectUrl`           | string?   | Redirection URL after signing.                        |
| `payload.documentMeta.signingOrder`          | string?   | Signing order (e.g., PARALLEL, SEQUENTIAL).           |
| `payload.documentMeta.language`              | string?   | The language for the document.                        |
| `payload.recipients`                         | array     | List of document recipients.                          |
| `payload.recipients.id`                      | number    | The id of the recipient.                              |
| `payload.recipients.email`                   | string    | Recipient's email address.                            |
| `payload.recipients.name`                    | string    | Recipient's name.                                     |
| `payload.recipients.role`                    | string    | Recipient's role (e.g., SIGNER, VIEWER, APPROVER).    |
| `payload.recipients.readStatus`              | string    | Document read status for the recipient.               |
| `payload.recipients.signingStatus`           | string    | Signing status for the recipient.                     |
| `payload.recipients.sendStatus`              | string    | Email delivery status for the recipient.              |
| `payload.recipients.signingOrder`            | number?   | The signing order for the recipient.                  |
| `payload.recipients.authOptions`             | json?     | Authentication options for the recipient.             |
| `payload.fields`                             | array     | List of document fields.                              |
| `payload.fields.id`                          | number    | The id of the field.                                  |
| `payload.fields.type`                        | string    | Field type (e.g., SIGNATURE, NAME, DATE).             |
| `payload.fields.pageNumber`                  | number    | Page number where the field is located.               |
| `payload.fields.pageX`                       | decimal   | X-coordinate for field position.                      |
| `payload.fields.pageY`                       | decimal   | Y-coordinate for field position.                      |
| `payload.fields.width`                       | decimal   | Width of the field.                                   |
| `payload.fields.height`                      | decimal   | Height of the field.                                  |
| `payload.fields.customText`                  | string?   | Custom text for the field.                            |
| `payload.fields.hint`                        | string?   | Hint text for the field.                              |
| `payload.fields.recipientId`                 | number?   | The id of the recipient assigned to the field.        |
| `payload.fields.fieldMeta`                   | json?     | Metadata for the field.                               |
| `payload.documentData.id`                    | string    | The identifier for document data.                     |
| `payload.documentData.type`                  | string    | The type of document data.                            |
| `payload.documentData.data`                  | string    | Base64-encoded document data.                         |
| `payload.documentData.initialData`           | string    | Base64-encoded initial document data.                 |
| `payload.team`                               | object?   | Details of the team that owns the document.           |
| `payload.user`                               | object    | Details of the user who owns the document.            |
| `payload.user.id`                            | number    | The id of the user.                                   |
| `payload.user.email`                         | string    | User's email address.                                 |
| `payload.user.name`                          | string?   | User's name.                                          |
| `payload.user.url`                           | string?   | User's profile URL.                                   |

## Verifying webhook signatures

If you provide a secret key when creating a webhook subscription, Davinci Sign signs the payload and includes the signature in the `X-Davinci-Secret` header of the request. To verify the signature, you need to calculate the HMAC of the payload using the secret key and compare it with the signature in the header.

The signature is calculated using the HMAC-SHA256 algorithm. Here is an example of how to verify the signature in Node.js:

```javascript
const crypto = require('crypto');

function verifySignature(payload, secret, signature) {
  const hmac = crypto.createHmac('sha256', secret);
  hmac.update(payload);
  const calculatedSignature = hmac.digest('hex');
  return calculatedSignature === signature;
}
```
